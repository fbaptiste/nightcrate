---
name: start-version
description: Use when a PR has been merged and you're ready to start work on the next version — checks out main, pulls latest, creates a new feature branch
---

# start-version

**Follow [`.claude/skills/start-version/SKILL.md`](../../../.claude/skills/start-version/SKILL.md).**
That file is the single source for this skill; this one is a pointer so the two
cannot drift.

Read it and do what it says, with one substitution: wherever it names a specific
agent — commit attribution, PR test-plan checkmarks, a `~/.claude/...` path —
use your own identity and your own equivalent path. See [`AGENTS.md`](../../../AGENTS.md).

If this skill needs a real change, change it in `.claude/skills/start-version/SKILL.md`.
